{
  "name": "joseli.to",
  "version": "0.2",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "tiddlywiki --listen",
    "build": "rm -rf .cache && tiddlywiki --build index",
    "minify": "node scripts/cleaner.js && html-minifier output/index.min.html -o output/index.html --remove-comments --collapse-whitespace --minify-css"
  },
  "dependencies": {
    "clean-css": "^5.1.5",
    "html-minifier": "^4.0.0",
    "node-html-parser": "^4.1.4",
    "roadroller": "^2.0.0",
    "tiddlywiki": "^5.1.23"
  }
}
